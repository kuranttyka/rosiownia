---
import Layout from '../layouts/Layout.astro'
import RPGCat from '../components/RPGCat'
---

<Layout title="Rouzeris">
  <main class="bento-wrap">
    <!-- Decorative scattered elements -->
    <div class="deco deco-star" style="top: 120px; left: 5%;"></div>
    <div class="deco deco-dot" style="top: 300px; right: 8%;"></div>
    <div class="deco deco-star" style="top: 550px; left: 3%;"></div>
    <div class="deco deco-dot" style="top: 780px; right: 4%;"></div>
    <div class="deco deco-sparkle" style="top: 200px; right: 15%;"></div>
    <div class="deco deco-sparkle" style="top: 650px; left: 10%;"></div>

    <div class="bento">

      <!-- ═══ Hero — spans 2 cols ═══ -->
      <div class="card hero-card col-span-2">
        <div class="hero-inner">
          <h1 class="hero-title">Rouzeris</h1>
          <p class="hero-sub">artist · jewelry maker · developer</p>
          <div class="hero-doodle">✦ ✧ ✦</div>
        </div>
      </div>

      <!-- ═══ About Me — tall, right column ═══ -->
      <div class="card about-card row-span-2">
        <div class="card-pad">
          <h2 class="card-title title-script">About Me</h2>
          <div class="about-avatar">🌸</div>
          <p class="about-text">
            Hi, I'm Rosia! I make jewelry from recycled silver, watch too much anime, and build things on the internet.
          </p>
          <p class="about-text mt-3">
            Based in Poland. Fueled by matcha and Studio Ghibli soundtracks. Currently learning wire wrapping and Rust.
          </p>
          <div class="tag-row">
            <span class="tag tag-rose">she/her</span>
            <span class="tag tag-lavender">INTJ</span>
            <span class="tag tag-sage">♓</span>
          </div>
        </div>
      </div>

      <!-- ═══ Anime Library — spans 2 cols ═══ -->
      <div class="card anime-card col-span-2">
        <div class="card-pad">
          <h2 class="card-title title-fun">📺 Anime Library</h2>
          <div class="anime-scroll">
            {[
              { title: 'Spy x Family', color: '#e8c4c4' },
              { title: 'Chainsaw Man', color: '#c4c4e8' },
              { title: 'Jujutsu Kaisen', color: '#c4dce8' },
              { title: 'My Dress-Up Darling', color: '#e8d4c4' },
              { title: 'Bocchi the Rock!', color: '#d4e8c4' },
              { title: 'Frieren', color: '#c4e8d8' },
              { title: 'Violet Evergarden', color: '#d8c4e8' },
              { title: 'Attack on Titan', color: '#e8c4c4' },
            ].map((a) => (
              <div class="anime-item">
                <div class="anime-cover" style={`background:${a.color};`}></div>
                <span class="anime-name">{a.title}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- ═══ Yuma — small cozy ═══ -->
      <div class="card yuma-card">
        <div class="card-pad text-center">
          <h2 class="card-title title-script">Meet Yuma 🐱</h2>
          <div class="yuma-photo"><span style="font-size:3rem;">🐈‍⬛</span></div>
          <p class="yuma-desc">
            Chief nap officer. Professional keyboard-sitter. Will steal your chair the moment you stand up.
          </p>
          <p class="yuma-link">
            <a href="https://instagram.com/jumabestia" target="_blank" rel="noopener" style="text-decoration: none; font-size: 1.5rem;">📷</a>
          </p>
        </div>
      </div>

      <!-- ═══ Jewelry — spans 2 cols ═══ -->
      <div class="card jewelry-card col-span-2">
        <div class="card-pad">
          <h2 class="card-title title-elegant">✨ Jewelry</h2>
          <div class="jewelry-grid">
            {[
              { name: 'silver ring', color: '#c8c0b8' },
              { name: 'pearl necklace', color: '#e8e0d4' },
              { name: 'amber earrings', color: '#d4a864' },
              { name: 'beaded bracelet', color: '#b8968c' },
              { name: 'wire pendant', color: '#a0a0a0' },
              { name: 'crystal brooch', color: '#c4d4e0' },
            ].map((item) => (
              <div class="jewelry-item">
                <div class="jewelry-thumb" style={`background:${item.color};`}></div>
                <span class="jewelry-name">{item.name}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- ═══ Listening ═══ -->
      <div class="card spotify-card">
        <div class="card-pad">
          <h2 class="card-title title-fun">🎵 Listening</h2>
          <div class="song-list">
            {[
              { track: 'Stefania', artist: 'Kalush Orchestra', color: '#e8c4a4' },
              { track: 'Щедрик', artist: 'Pentatonix', color: '#a4c4e8' },
              { track: 'Glimpse of Us', artist: 'Joji', color: '#c4a4c8' },
              { track: "Cruel Angel's Thesis", artist: 'Yoko Takahashi', color: '#e8a4a4' },
              { track: 'Homura', artist: 'LiSA', color: '#a4e8c4' },
            ].map((s) => (
              <div class="song-row">
                <div class="song-art" style={`background:${s.color};`}></div>
                <div>
                  <div class="song-track">{s.track}</div>
                  <div class="song-artist">{s.artist}</div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- ═══ Blog — spans 2 cols ═══ -->
      <div class="card blog-card col-span-2">
        <div class="card-pad">
          <h2 class="card-title title-elegant">📝 Blog</h2>
          <div class="blog-grid">
            {[
              { title: 'Making jewelry with recycled silver', color: '#c8c0b8' },
              { title: 'My top 10 anime of 2025', color: '#c4c4e8' },
              { title: 'Building apps with AI', color: '#c4dce8' },
            ].map((post) => (
              <div class="blog-item">
                <div class="blog-thumb" style={`background:${post.color};`}></div>
                <h3 class="blog-title">{post.title}</h3>
                <p class="blog-more">Read more →</p>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- ═══ Instagram ═══ -->
      <div class="card insta-card">
        <div class="card-pad">
          <h2 class="card-title title-fun">📸 Instagram</h2>
          <div class="insta-links">
            <a href="https://instagram.com/roksolanas_7" target="_blank" rel="noopener" class="insta-link">
              📷 @roksolanas_7
              <span class="insta-sub">personal</span>
            </a>
            <a href="https://instagram.com/jumabestia" target="_blank" rel="noopener" class="insta-link">
              🐱 @jumabestia
              <span class="insta-sub">Yuma the cat</span>
            </a>
          </div>
        </div>
      </div>

    </div>
  </main>

  <!-- Footer -->
  <footer class="site-footer">
    <div class="footer-inner">
      <div class="footer-links">
        <a href="https://instagram.com/roksolanas_7" target="_blank" rel="noopener">instagram</a>
        <span class="footer-dot">·</span>
        <a href="https://github.com/rouzeris" target="_blank" rel="noopener">github</a>
        <span class="footer-dot">·</span>
        <a href="https://instagram.com/jumabestia" target="_blank" rel="noopener">yuma</a>
      </div>
      <p class="footer-note">made with ♥ and too much matcha</p>
    </div>
  </footer>

  <RPGCat client:load />
</Layout>

<style>
  /* ═══════════════════════════════════════
     LAYOUT
     ═══════════════════════════════════════ */
  .bento-wrap {
    max-width: 1200px;
    margin: 0 auto;
    padding: 32px 20px 40px;
    position: relative;
  }

  .bento {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
  }

  .col-span-2 { grid-column: span 2; }
  .row-span-2 { grid-row: span 2; }

  @media (max-width: 768px) {
    .bento {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }
  }

  /* ═══════════════════════════════════════
     BASE CARD
     ═══════════════════════════════════════ */
  .card {
    border-radius: 16px;
    box-shadow:
      0 1px 3px rgba(61, 44, 30, 0.06),
      0 4px 16px rgba(61, 44, 30, 0.04);
    transition:
      transform 0.3s cubic-bezier(.4,0,.2,1),
      box-shadow 0.3s ease;
    overflow: hidden;
    position: relative;
  }
  .card:hover {
    transform: translateY(-3px) scale(1.008);
    box-shadow:
      0 4px 12px rgba(61, 44, 30, 0.1),
      0 12px 32px rgba(61, 44, 30, 0.07);
  }

  .card-pad { padding: 24px; }

  /* ═══════════════════════════════════════
     CARD VARIANTS
     ═══════════════════════════════════════ */
  .hero-card {
    background: linear-gradient(135deg, #fefcf8 0%, #f5ece0 50%, #f0e4d4 100%);
    border: 1px solid rgba(139, 111, 71, 0.1);
  }
  .about-card {
    background: #fefcf8;
    border: 1px dashed rgba(180, 156, 252, 0.4);
  }
  .about-card:hover {
    border-color: rgba(180, 156, 252, 0.7);
    box-shadow: 0 4px 20px rgba(180, 156, 252, 0.12);
  }
  .anime-card {
    background: linear-gradient(180deg, #fefcf8 0%, #f8f0ff 100%);
  }
  .yuma-card {
    background: #fefcf8;
    background-image:
      radial-gradient(circle at 20% 80%, rgba(232, 160, 180, 0.08) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(212, 168, 83, 0.08) 0%, transparent 50%);
    border: 1px solid rgba(212, 168, 83, 0.15);
  }
  .yuma-card:hover {
    transform: translateY(-3px) rotate(-0.5deg) scale(1.01);
    border-color: rgba(212, 168, 83, 0.35);
  }
  .jewelry-card {
    background: #fefcf8;
    background-image: radial-gradient(circle at 2px 2px, rgba(139, 111, 71, 0.04) 1px, transparent 1px);
    background-size: 16px 16px;
  }
  .spotify-card {
    background: linear-gradient(160deg, #fefcf8 0%, #f0f5ee 100%);
  }
  .blog-card {
    background: #fefcf8;
  }
  .insta-card {
    background: linear-gradient(135deg, #fefcf8 0%, #fff0f4 50%, #fef0e8 100%);
  }

  /* ═══════════════════════════════════════
     TITLE STYLES
     ═══════════════════════════════════════ */
  .card-title {
    font-size: 1.1rem;
    font-weight: 500;
    color: #3d2c1e;
    margin: 0 0 16px;
    letter-spacing: 0.02em;
  }
  .title-script {
    font-family: 'Crimson Pro', serif;
    font-size: 1.4rem;
    font-weight: 700;
    font-style: italic;
  }
  .title-fun {
    font-size: 1.05rem;
    font-weight: 600;
    letter-spacing: 0.04em;
  }
  .title-elegant {
    font-family: 'Crimson Pro', serif;
    font-size: 1.25rem;
    font-weight: 400;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  /* ═══════════════════════════════════════
     HERO
     ═══════════════════════════════════════ */
  .hero-inner {
    padding: 52px 44px;
  }
  .hero-title {
    font-family: 'Crimson Pro', serif;
    font-size: clamp(2.8rem, 6vw, 4.5rem);
    font-weight: 300;
    color: #3d2c1e;
    margin: 0;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }
  .hero-sub {
    font-size: 1.15rem;
    color: #8B6F47;
    margin-top: 12px;
    letter-spacing: 0.08em;
  }
  .hero-doodle {
    margin-top: 20px;
    font-size: 0.85rem;
    color: #c4b09a;
    letter-spacing: 0.3em;
  }

  /* ═══════════════════════════════════════
     ABOUT
     ═══════════════════════════════════════ */
  .about-avatar {
    width: 72px;
    height: 72px;
    border-radius: 50%;
    background: linear-gradient(135deg, #f5ece0, #ffe8ee);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    margin: 0 auto 16px;
  }
  .about-text {
    font-size: 0.9rem;
    color: #5a4a3e;
    line-height: 1.65;
    margin: 0;
  }
  .tag-row {
    display: flex;
    gap: 8px;
    margin-top: 16px;
    flex-wrap: wrap;
  }
  .tag {
    font-size: 0.75rem;
    padding: 4px 10px;
    border-radius: 20px;
    font-weight: 500;
  }
  .tag-rose     { background: rgba(232, 160, 180, 0.2); color: #a0607a; }
  .tag-lavender { background: rgba(180, 156, 252, 0.2); color: #6a56b0; }
  .tag-sage     { background: rgba(109, 158, 122, 0.2); color: #4a7a58; }

  /* ═══════════════════════════════════════
     ANIME
     ═══════════════════════════════════════ */
  .anime-scroll {
    display: flex;
    gap: 14px;
    overflow-x: auto;
    padding-bottom: 8px;
    scroll-snap-type: x mandatory;
  }
  .anime-item {
    flex-shrink: 0;
    width: 120px;
    scroll-snap-align: start;
  }
  .anime-cover {
    width: 120px;
    height: 170px;
    border-radius: 8px;
  }
  .anime-name {
    font-size: 0.85rem;
    color: #3d2c1e;
    display: block;
    margin-top: 6px;
    line-height: 1.2;
  }

  /* ═══════════════════════════════════════
     YUMA
     ═══════════════════════════════════════ */
  .yuma-photo {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    background: rgba(212, 168, 83, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 8px auto;
    border: 2px dashed rgba(212, 168, 83, 0.25);
  }
  .yuma-desc {
    font-size: 0.9rem;
    color: #6b5a4e;
    line-height: 1.5;
    margin-top: 12px;
  }
  .yuma-link {
    font-size: 0.8rem;
    color: #8B6F47;
    margin-top: 8px;
    font-style: italic;
  }
  .yuma-link a {
    color: #b49cfc;
    text-decoration: underline;
  }

  /* ═══════════════════════════════════════
     JEWELRY
     ═══════════════════════════════════════ */
  .jewelry-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
  }
  .jewelry-item {
    cursor: pointer;
    transition: transform 0.2s ease, filter 0.2s ease;
  }
  .jewelry-item:hover {
    transform: scale(1.06);
    filter: brightness(1.05);
  }
  .jewelry-thumb {
    aspect-ratio: 1;
    border-radius: 10px;
    margin-bottom: 6px;
  }
  .jewelry-name {
    font-size: 0.82rem;
    color: #6b5a4e;
    text-align: center;
    display: block;
  }

  /* ═══════════════════════════════════════
     SONGS
     ═══════════════════════════════════════ */
  .song-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .song-row {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 6px 8px;
    border-radius: 8px;
    transition: background 0.15s ease;
  }
  .song-row:hover { background: rgba(109, 158, 122, 0.08); }
  .song-art {
    width: 36px;
    height: 36px;
    border-radius: 6px;
    flex-shrink: 0;
  }
  .song-track {
    font-size: 0.85rem;
    color: #3d2c1e;
    line-height: 1.2;
  }
  .song-artist {
    font-size: 0.72rem;
    color: #8B6F47;
  }

  /* ═══════════════════════════════════════
     BLOG
     ═══════════════════════════════════════ */
  .blog-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
  .blog-item {
    cursor: pointer;
    transition: transform 0.2s ease;
  }
  .blog-item:hover { transform: translateY(-3px); }
  .blog-thumb {
    height: 80px;
    border-radius: 8px;
    margin-bottom: 10px;
  }
  .blog-title {
    font-size: 0.95rem;
    color: #3d2c1e;
    margin: 0;
    font-weight: 500;
    line-height: 1.3;
  }
  .blog-more {
    font-size: 0.8rem;
    color: #8B6F47;
    margin-top: 4px;
  }

  /* ═══════════════════════════════════════
     INSTAGRAM
     ═══════════════════════════════════════ */
  .insta-links {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-top: 8px;
  }
  .insta-link {
    display: block;
    padding: 12px 16px;
    background: rgba(250, 243, 232, 0.6);
    border-radius: 10px;
    color: #3d2c1e;
    text-decoration: none;
    font-size: 0.95rem;
    transition: background 0.15s ease, transform 0.15s ease;
  }
  .insta-link:hover {
    background: #f0e6d6;
    transform: translateX(4px);
  }
  .insta-sub {
    font-size: 0.8rem;
    color: #8B6F47;
    display: block;
    margin-top: 2px;
  }

  /* ═══════════════════════════════════════
     SCROLLBAR
     ═══════════════════════════════════════ */
  .card ::-webkit-scrollbar { height: 4px; width: 4px; }
  .card ::-webkit-scrollbar-track { background: transparent; }
  .card ::-webkit-scrollbar-thumb { background: rgba(139, 111, 71, 0.2); border-radius: 2px; }

  /* ═══════════════════════════════════════
     DECORATIVE
     ═══════════════════════════════════════ */
  .deco {
    position: fixed;
    pointer-events: none;
    z-index: 0;
    opacity: 0.25;
  }
  .deco-star::before {
    content: '✦';
    font-size: 1.2rem;
    color: #d4a853;
    animation: twinkle 3s ease-in-out infinite;
  }
  .deco-dot::before {
    content: '●';
    font-size: 0.5rem;
    color: #b49cfc;
    animation: twinkle 4s ease-in-out infinite 1s;
  }
  .deco-sparkle::before {
    content: '✧';
    font-size: 0.9rem;
    color: #e8a0b4;
    animation: twinkle 3.5s ease-in-out infinite 0.5s;
  }
  @keyframes twinkle {
    0%, 100% { opacity: 0.2; }
    50% { opacity: 0.8; }
  }

  /* ═══════════════════════════════════════
     FOOTER
     ═══════════════════════════════════════ */
  .site-footer {
    padding: 40px 20px 100px;
    text-align: center;
  }
  .footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    border-top: 1px solid rgba(139, 111, 71, 0.12);
    padding-top: 28px;
  }
  .footer-links {
    display: flex;
    justify-content: center;
    gap: 8px;
    align-items: center;
    flex-wrap: wrap;
  }
  .footer-links a {
    color: #8B6F47;
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.15s ease;
  }
  .footer-links a:hover { color: #b49cfc; }
  .footer-dot {
    color: #c4b09a;
    font-size: 0.8rem;
  }
  .footer-note {
    font-size: 0.8rem;
    color: #b8a898;
    margin-top: 12px;
    font-style: italic;
  }

  /* ═══════════════════════════════════════
     RESPONSIVE: BLOG GRID
     ═══════════════════════════════════════ */
  @media (max-width: 768px) {
    .blog-grid {
      grid-template-columns: 1fr;
    }
    .jewelry-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
